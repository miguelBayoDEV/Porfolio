package NumeroPar_Ejer12;

public class Par {

	// Uso del if ternario
	public static String isPar(int numero) {
		return (numero % 2 == 0) ? "Número " + numero + " es par." : "Número " + numero + " es impar.";
	}
}
