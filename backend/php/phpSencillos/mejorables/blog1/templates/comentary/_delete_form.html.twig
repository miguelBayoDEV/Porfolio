<form method="post" action="{{ path('comentary_delete', {'id': comentary.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comentary.id) }}">
    <button class="btn">Delete</button>
</form>
